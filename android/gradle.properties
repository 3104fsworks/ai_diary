org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Djava.net.preferIPv4Stack=true -Djava.io.tmpdir=C:/GradleTmp -Djava.rmi.server.hostname=127.0.0.1
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# The previous WEPollSelector loopback bug only affected JDK 21 on Windows
# Insider builds. With JDK 17 (set via `flutter config --jdk-dir`) the
# default Daemon + parallel mode works again.
